<!--p id="notice"><%= notice %></p-->

<% unless @page.hide_title %>
  <h1>	
	  <%= @page.title %>
  </h1>
<% end %>

<div>
  <%= formatize @page.content %>
</div>

<p>
<%= link_to 'Edit', edit_page_path(@page) if session[:admin] %>
<% if request.path.include?('admin') %>
  | <%= link_to 'Show public', public_page_path(@page) if session[:admin] %>
<% end %>

</p>